# 学习历程

认真地看了学长讲解的视频，自己跟着推导了那些运动学的公式。然后就直接上手写代码了

---

先看了作业1的第1问。这个疑似就是把刚推的公式套进去就好了。
$$V_{x_B} = \frac{dX_B}{dt} = V_x cos\psi$$
$$V_{y_B} = \frac{dY_B}{dt} = V_y cos\psi$$
$$\omega = \frac{d\psi}{dt} = \frac{V_y}{L} = \frac{V_x  tan\delta}{L}$$

我们需要算出来车在全局坐标系下的坐标$X_B , Y_B$以及车的航向角$\psi$
有$\displaystyle\frac{dX_B}{dt} = V_{x_B}$，将其离散化则得到${X_B}_k = {X_B}_{k-1} + V_{x_B} * \Delta t$

不出意外地，我一开始忘把$\Delta t$乘进去了。虽然画出来还是个⚪，但半径有几百米。。。

---

然后是第2问。我一开始想用PID控制转向角，但没有什么好的思路。把整个路径上的每个点处的转向角都算出来，和当前的转向角做误差，然后做PID。出于神秘原因，这个做得很辣鸡。为了把锅diu给PID，我选择了用纯跟踪

为了在路径上面找距离刚好是前视距离的点，索性把全部点的距离都算了出来，一个个找
这次照搬公式的时候坏事了。一开始我写的 $err = Y_{tar} - Y $，想当然地以为俩纵坐标减一下就是纵向误差了，完全没发现这是在全局坐标系下的。调了半天的前视距离，路径还是震荡。后面问了下ai才恍然大悟。

---

作业2最大的困难不出意外的是配置环境。。。
第1问也是主要看公式。自己知道而且能理解公式的话写代码就很快。
而且好像没有怎么用到eigen库的功能？只用到了eigenVector的乘号重载

第2问也主要是看公式。跟微积分讲的拉格朗日乘数法是差不多的，重点在不等式约束。约束是>=0还是<=0会决定在约束上找最小值的梯度方向。然后$\mu g(x)>=0$会分成在约束范围内找还是在约束上找

第3问对OSQP库的应用主要是在创建矩阵上，这一部分无招遂求助ai了，让ai写了代码，自己再对着看，尝试自己写来复现。其中，创建矩阵要用CSC格式的矩阵，用非零元素，其对应的行，每列首个非零元素在前两个数组中的索引，这三个数组来储存矩阵的信息。求解反而只是把参数传进去就好了。